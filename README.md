# OpenSSL password hash

> [!Note]
> This repo has been archived -- the script can now be found in the [bin-linux](https://github.com/DavidVogelxyz/bin-linux) repo.

This simple script will assist the user with generating password hashes with `openssl passwd`.

Full command:

```
openssl passwd -salt <SALT> -6
```
